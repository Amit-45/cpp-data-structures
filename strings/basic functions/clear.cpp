//Erases the contents of the string, which becomes an empty string (with a length of 0 characters). 
#include <bits/stdc++.h>
using namespace std;
int main()
{
    string str = "abc";

    str.clear();
    cout << str;
}
//Output: A string with 0 characters
//Syntax-->(string  to be cleared).clear();
