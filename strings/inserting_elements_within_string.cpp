//insert function is used for inserting elements  within a string
#include <bits/stdc++.h>
using namespace std;
int main()
{
    string str1 = "abef";
    cout << str1.insert(2, "cd");

    return 0;
}
//Syntax-->(string).insert(index,string to be inserted)
